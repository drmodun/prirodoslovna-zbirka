export enum InputType {
  PASSWORD,
  TEXT,
  FILE,
  NUMBER,
}

export enum ButtonColor {
  GREEN = "green",
  BLUE = "blue",
}

export enum InputResult {
  SUCCESS = "success",
  ERROR = "error",
  DEFAULT = "default",
}
